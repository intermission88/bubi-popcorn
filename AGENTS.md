# bubi-popcorn

Aplikasi web mobile-first (1 file): `index.html` — semua HTML/CSS/JS ada di sini (~1900 baris).
- Data film: Supabase (tabel `bubi_popcorn`), konfigurasi ada di bagian `<script>` bawah file.
- Aset gambar: `assets/` (jangan dibaca, file biner).
- Sinkron ke GitHub: jalankan `push.bat` (commit + push otomatis).
- Baca `index.html` HANYA dengan grep + offset/limit. Jangan baca utuh.
- Deploy via GitHub Pages.
